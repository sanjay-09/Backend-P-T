import { types } from "./types";
import { queries } from "./queries";
import { resolvers,extraResolvers,mutationResolvers } from "./resolvers";
import { mutation } from "./mutation";
export const user={
    types,
    queries,
    resolvers,
    extraResolvers,
    mutationResolvers,
    mutation
}